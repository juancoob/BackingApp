<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_margin_padding"
    android:padding="@dimen/default_margin_padding">

    <ImageView
        android:id="@+id/iv_recipe_ingredient_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/recipe_ingredient_icon_description"
        android:src="@drawable/ic_dot_black_10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_recipe_ingredient"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/default_margin_padding"
        android:layout_marginStart="@dimen/default_margin_padding"
        android:maxLines="2"
        android:textSize="@dimen/default_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/iv_recipe_ingredient_icon"
        app:layout_constraintStart_toEndOf="@+id/iv_recipe_ingredient_icon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/dummy_recipe_ingredient" />

</android.support.constraint.ConstraintLayout>

